{
  "name": "cron_list",
  "description": "List all cron jobs scheduled via CronCreate (session-only, or durable under ~/.qwen/tmp/<project-hash>/scheduled_tasks.json) and pending loop wakeups scheduled via LoopWakeup (always session-only).",
  "parametersJsonSchema": {
    "type": "object",
    "properties": {},
    "additionalProperties": false
  }
}
