{
  "name": "view_media",
  "note": "The schema field is the exact Amp CLI `tools show --json` definition observed for the listed mode(s).",
  "modes": [
    "deep",
    "large",
    "rush",
    "smart"
  ],
  "schema": {
    "name": "view_media",
    "description": "View or analyze a local media file.\n\nUse this when you need to inspect an image, PDF, audio file, or video. The path must be absolute.\n\nWhen no objective is provided for a PNG/JPEG/GIF/WebP image, this tool returns the image as visual input so you can inspect it directly. When objective is provided, or when viewing a PDF, audio file, or video, use it when you want a textual description or answer about the media; in this mode the tool returns text only and does not return an image block.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute path to the local media file."
        },
        "objective": {
          "type": "string",
          "description": "Optional objective to answer about the media. If provided, the tool returns a text-only answer instead of returning an image block."
        },
        "resolution": {
          "type": "string",
          "enum": [
            "high",
            "original"
          ],
          "description": "Optional image resolution preference. Use high to scale large raster images down when image compression support is available, or original to preserve the original image data."
        }
      },
      "required": [
        "path"
      ]
    },
    "source": "builtin"
  }
}
